services:
  - type: web
    name: ipl-dashboard
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app:app --workers 2 --threads 4
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.0
